<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dubbo.provider.mapper.UserMapper">

  <select id="selectAll" resultType="dubbo.common.pojo.User">
    SELECT *
    FROM user;
  </select>
</mapper>
